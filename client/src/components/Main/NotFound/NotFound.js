import React from "react";

const NotFound = () => {
  return <div>
    Lo siento, la pagina que intentas ver o no existe o no es accesible en este momento. Comprueba que la direccion de la barra de navegacion sea correcta. P.D Alex es un caraculo. XD
  </div>;
};

export default NotFound;
